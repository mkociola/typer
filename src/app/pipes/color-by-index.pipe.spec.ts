import { ColorByIndexPipe } from './color-by-index.pipe';

describe('ColorByIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorByIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
